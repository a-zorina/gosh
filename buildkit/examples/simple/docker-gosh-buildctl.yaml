# syntax=127.0.0.1:5000/buildkit-gosh

apiVersion: 1
steps:
  - name: print date
    run:
      image: bash:latest
      command: ["/usr/local/bin/bash"]
      args:
        - -c
        - >-
          (date +'%s %H:%M:%S %Z'; echo "Hi there") | tee /message.txt
